# Copy this file to ".env" (same folder) and fill in your real key.
#   cp .env.example .env
#
# Get a key at https://openrouter.ai/  (Account -> Keys).
# Add credits or enable auto top-up, or model calls will fail with 402.
OPENROUTER_API_KEY=sk-or-v1-replace-me
